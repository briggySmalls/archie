language: go

jobs:
  include:
      - stage: lint
        script: make check
      - stage: lint
        script: make lint
      - stage: lint
        script: make vet
      - stage: test
        script: make test

install:
  - go mod download

env:
  - GO111MODULE=on
